# This directory holds UCI configs backed up from the router.
# Run backup-router-configs.sh to populate it, or use:
#   ssh root@192.168.31.1 'cd /etc/config && tar cf - .' | tar xf - -C files/etc/config/
# Contents here are baked into the firmware rootfs at build time.
